import { fonts, fontSizes, fontWeights } from "./artifacts/fonts";
import { installationIcons } from "./artifacts/icons";
import { sizes } from "./artifacts/sizes";
import { lineHeights, space } from "./artifacts/spacing";
import { palette } from "./artifacts/palette";

export const theme = {
  fonts,
  fontSizes,
  fontWeights,
  installationIcons,
  lineHeights,
  sizes,
  space,
  palette,
};
